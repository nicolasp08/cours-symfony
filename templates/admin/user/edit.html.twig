{% extends "admin/base.html.twig" %}

{% block title %}
    Modification de l'utilisateur {{user.id}}
{% endblock title %}

{% block body %}
    
<div class="container-fluid">
    <h1 class="my-5">Modification du l'utilisateur {{user.fullName}}</h1>
    <div class="alert bg-info" style="color:white">
        <strong>Nom Prénom :  </strong> {{user.fullName}}<br>
        <strong>Email : </strong> {{user.email}}<br>
    </div>
    {{ form_start(form) }}

    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary"><i class="fas fa-check"></i>  Enregistrer les modifications</button>
    {{ form_end(form) }}

    
</div>

{% endblock body %}

